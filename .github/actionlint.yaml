self-hosted-runner:
  labels:
    # GitHub-hosted public preview label announced in actions/runner-images#14404.
    # This supports local/pre-review actionlint runs. Remove the override after
    # actionlint recognizes the hosted label.
    - xcode-27
